# URL base da API. Obrigatoria: sem ela o app falha na inicializacao com uma
# mensagem explicita, em vez de tentar uma origem errada em silencio.
#
# Em desenvolvimento, casa com o PORT de back-end/.env (3000).
# Em producao, aponte para a URL publica da API.
VITE_API_URL=http://localhost:3000
